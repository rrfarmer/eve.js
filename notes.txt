###########################
###    MAJOR UPDATES    ###
###########################

i successfully patched blue.dll!
i changed start.ini in the client directory to use Placebo encoding, instead of CryptoApi.
this makes it much easier to freely read packets as they are received.

!! if you need to use a module that is not natively imported with the client, use the FULL patched
for example: instead of util.Rowset use: eve.common.script.sys.rowset.Rowset 
(you can find this in eve client files)


###########################
###     DISCOVERIES     ###
###########################
whenever the SERVER logs [LOG]: [CallReq] alert::BeanCount(), it means the client has crashed and is sending an error report
if client gives error: Formatted exception info: TypeError: 'NoneType' object is not iterable, that means that service is not implemented yet
return {
  type: "object",
  name: "util.KeyVal",
  args: {
    type: "dict",
    entries: [["Index", { type: "token", value: "util.Row" }]],
  },
};
the above returns an object that has an Index property, which is a token "util.Row".
this solves the issue where the client says ... has no attribute 'Index'


###########################
###     TO-DO LIST      ###
###########################

- fix: when i leave current ship (to go to pod) then reboard my ship, pod stays in inventory...
- if user signs in on different client, and user is already active in another client, kick active user off.
- fix all stations orientation. they all face same direction.
- sort the service files and place them in the correct folders
- (50%) skill plan server (https)
- (50%) factionwf server (https)
- (50% done) (image server works, paperdolls not) set up image server + make paperdolls work
- make a universal run script which does 3 things:
    1: check if chat certificate is installed, if not, ask user if they would like to install it.
    2: check if dependencies are installed, if not, ask user if they would like to install them.
    3: start server

###########################
###   BIG TO-DO LIST    ###
###########################

station services ((50%) fitting, market... etc)
(50%) warping

###########################
###   WHERE I LEFT OFF  ###
###########################
implement services: 
shipCosmeticsMgr::GetEnabledCosmetics()
chatAuthenticationService::GetAuthenticationToken()
skillMgr::PurchaseSkills()
keeper::GetCurrentDungeonForCharacter()
insurgencySolarsystem::GetLocalCampaignClientSnapshot()
bountyProxy::GetBountiesAndKillRights()
scanMgr::SignalTrackerRegister()
essMgr::GetDataForClientSolarSystem()
achievementTrackerMgr::HasEverWarped()